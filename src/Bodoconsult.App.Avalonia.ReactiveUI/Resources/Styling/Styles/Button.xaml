<models:SharedResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                 xmlns:models="clr-namespace:Bodoconsult.App.Avalonia.Models;assembly=Bodoconsult.App.Avalonia">

    <models:SharedResourceDictionary.MergedDictionaries>
        <models:SharedResourceDictionary Source="/Bodoconsult.App.Avalonia.ReactiveUI;component/Resources/Styling/BrushesColors/Colors.xaml" />
        <models:SharedResourceDictionary Source="/Bodoconsult.App.Avalonia.ReactiveUI;component/Resources/Styling/BrushesColors/Brushes.xaml" />
        <models:SharedResourceDictionary Source="/Bodoconsult.App.Avalonia.ReactiveUI;component/Resources/Styling/Typography/Typography.xaml" />
        <models:SharedResourceDictionary Source="/Bodoconsult.App.Avalonia.ReactiveUI;component/Resources/Styling/Styles/FrameworkElement.xaml" />
    </models:SharedResourceDictionary.MergedDictionaries>

    <Style x:Key="ButtonStyle" TargetType="Button" BasedOn="{StaticResource FrameworkElementStyle}">
        <Setter Property="Background" Value="{StaticResource CommandBarButtonBackgroundBrush01 }" />
        <Setter Property="Foreground" Value="{StaticResource CommandBarButtonForegroundBrush01 }" />
        <Setter Property="BorderBrush" Value="{StaticResource CommandBarButtonBorderBrush01 }" />
        <Setter Property="BorderThickness" Value="0 0 0 5" />
        <Setter Property="Padding" Value="10 0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{StaticResource Level3FontSize }" />
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}" />
        <Setter Property="FontWeight" Value="{StaticResource NormalFontWeight}" />
        <!--<Setter Property="MinWidth" Value="100"/>-->
        <Setter Property="MaxHeight" Value="35"/>
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Opacity=".8" />
                        <ContentPresenter x:Name="Content"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Padding" Value="10 2 10 0" />
            </Trigger>
        </Style.Triggers>-->
    </Style>

    <!---->

    <Style x:Key="CommandBarButtonStyle01" TargetType="Button" BasedOn="{StaticResource FrameworkElementStyle}">
        
        <Setter Property="MinHeight" Value="100" />
        
        <Setter Property="Background" Value="{StaticResource CommandBarButtonBackgroundBrush01 }" />
        <Setter Property="Foreground" Value="{StaticResource CommandBarButtonForegroundBrush01 }" />
        <Setter Property="BorderBrush" Value="{StaticResource CommandBarButtonBorderBrush01 }" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="BorderThickness" Value="3" />
        
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="FontSize" Value="{StaticResource Level3FontSize }" />
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}" />
        <Setter Property="FontWeight" Value="{StaticResource NormalFontWeight}" />
        
        <Setter Property="Padding" Value="0" />
        
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Height="{TemplateBinding Height}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Opacity="1" 
                                CornerRadius="10"/>
                        <ContentPresenter x:Name="Content"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Padding" Value="10 2 10 0" />
            </Trigger>
            
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource CommandBarButtonMouseOverBackgroundBrush01 }" />
            </Trigger>
        </Style.Triggers>
    </Style>

 <Style x:Key="DefaultButtonStyle01" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
        <Setter Property="FontSize" Value="{StaticResource Level3FontSize }" />
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}" />
        <Setter Property="FontWeight" Value="{StaticResource BoldFontWeight}" />
        <Setter Property="MinWidth" Value="150" />
     <!--<Setter Property="Foreground" Value="{StaticResource ButtonForegroundBrush02}"/>-->
          <Setter Property="MaxWidth" Value="500" />
        <!--<Setter Property="Margin" Value="5 0 5 0" />-->

               <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                            <Border BorderBrush="White" BorderThickness="1" CornerRadius="3"
                                    MinWidth="150" MaxWidth="500" MaxHeight="40">
            <Border BorderBrush="DarkGray" BorderThickness="1" CornerRadius="3">
                <Grid Name="ButtonGrid">
                    <Rectangle Name="ButtonRect">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Offset="0" Color="LightGray" />
                                <GradientStop Offset="0.49" Color="Orange" />
                                <GradientStop Offset="0.5" Color="Orange" />
                                <GradientStop Offset="1" Color="{StaticResource BackgroundColor01 }" />
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <ContentPresenter Margin="10 0 10 0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="Auto" />
                </Grid>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <!-- Bei Mausbewegung über den Button ... -->
            <Trigger Property="IsMouseOver" Value="True">
                <!-- Buttonhintergrund heller darstellen -->
                <Setter Property="Fill" TargetName="ButtonRect">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Offset="0" Color="Silver" />
                    <GradientStop Offset="1" Color="{ DynamicResource HighlightColor }" />
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <!-- Leuchteffekt -->
                <Setter Property="BitmapEffect">
                    <Setter.Value>
                        <OuterGlowBitmapEffect GlowColor="White" GlowSize="5" />
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Fill" TargetName="ButtonRect" Value="{ DynamicResource HighlightBrush }" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    </Setter.Value>
    </Setter>
    <!--<Style.Triggers>
        <Trigger Property="IsPressed" Value="True">
            <Setter Property="Padding" Value="10 2 10 0" />
        </Trigger>

        <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Background" Value="{StaticResource CommandBarButtonMouseOverBackgroundBrush01 }" />
        </Trigger>
    </Style.Triggers>-->
    </Style>

</models:SharedResourceDictionary>