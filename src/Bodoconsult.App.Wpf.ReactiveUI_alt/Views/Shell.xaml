<controls:MetroWindow x:Class="Bodoconsult.App.Wpf.ReactiveUI.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:interactivity="http://www.codeplex.com/prism"
        xmlns:base="clr-namespace:Bodoconsult.Wpf.Base;assembly=Bodoconsult.Wpf.Base"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Shell" 
         Height="600" Width="800" ResizeMode="CanResizeWithGrip" 
        >



    <Grid>
                <Border>
                    <ContentControl x:Name="DocumentRegion"
                            interactivity:RegionManager.RegionName="DocumentRegion"
                            VerticalContentAlignment="Stretch"
                            HorizontalContentAlignment="Stretch" 
                                    />
                </Border>


        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"></ColumnDefinition>
            <ColumnDefinition Width="80*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0">
            <ContentControl x:Name="CommandRegion"
                            interactivity:RegionManager.RegionName="CommandRegion"
                            VerticalContentAlignment="Stretch"
                            HorizontalContentAlignment="Stretch" />
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="80*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Margin="15 15 15 15">
                <ContentControl x:Name="ChartRegion"
                            interactivity:RegionManager.RegionName="ChartRegion"
                            VerticalContentAlignment="Stretch"
                            HorizontalContentAlignment="Stretch" />

            </Border>

            <Border Grid.Row="1">
                <ContentControl x:Name="DocumentRegion"
                            interactivity:RegionManager.RegionName="DocumentRegion"
                            VerticalContentAlignment="Stretch"
                            HorizontalContentAlignment="Stretch" />
            </Border>
        </Grid>-->

    </Grid>
    

</controls:MetroWindow>
