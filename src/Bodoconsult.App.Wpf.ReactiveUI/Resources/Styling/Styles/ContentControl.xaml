<models:SharedResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:models="clr-namespace:Bodoconsult.App.Wpf.Models;assembly=Bodoconsult.App.Wpf">

    <models:SharedResourceDictionary.MergedDictionaries>
        <models:SharedResourceDictionary Source="/Bodoconsult.App.Wpf.ReactiveUI;component/Resources/Styling/BrushesColors/Brushes.xaml" />
        <models:SharedResourceDictionary Source="/Bodoconsult.App.Wpf.ReactiveUI;component/Resources/Styling/Styles/FrameworkElement.xaml" />
    </models:SharedResourceDictionary.MergedDictionaries>

    <!--<Style x:Key="CircleCutOutContentControlStyle" TargetType="ContentControl" BasedOn="{StaticResource FrameworkElementStyle}">
        <Setter Property="Background" Value="{StaticResource BaseWhiteBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BaseBlackBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Grid>
                        <Ellipse x:Name="Border" Fill="{TemplateBinding BorderBrush}" Margin="-1"/>
                        <Ellipse x:Name="Mask" Fill="{TemplateBinding Background}"/>
                        <ContentPresenter>
                            <ContentPresenter.OpacityMask>
                                <VisualBrush AlignmentX="Left" Stretch="None" AlignmentY="Top" Visual="{Binding ElementName=Mask}"/>
                            </ContentPresenter.OpacityMask>
                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

</models:SharedResourceDictionary>